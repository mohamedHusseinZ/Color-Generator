import React from 'react';

const Home = () => {
  return (
    <div>
      <h1>Welcome to Color Generator</h1>
      <p>
        This application allows you to generate random colors and convert them between different color models. Use the buttons to generate a new random color or convert the displayed color to different formats such as RGB and HSL.
      </p>
    </div>
  );
};

export default Home;
